var base = '10';
var altezza = '5';
var area = null;
area = ((Number(base) * Number(altezza))/2);

if(Number(area))
    console.log('L\'area del trinagolo di base: ' + base + ' e altezza: ' + altezza + ' è: '+ area);
else
    console.log('Input errato!');
